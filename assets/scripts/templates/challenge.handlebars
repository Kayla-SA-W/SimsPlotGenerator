{{#with challenge as |challenge|}}
<section id='singleChallenge' data-id="{{challenge.id}}">
  <h3 class='title'>
    {{challenge.title}}
  </h3>
  <h6> You can find the rules to the challenge here:
    <a href="{{challenge.link}}" target=_blank>{{challenge.link}}</a>
  </h6>
</section>
{{/with}}
